package com.msh.gasapp.repository;

import com.msh.gasapp.model.GasReader;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GasReaderRepository extends JpaRepository<GasReader, Integer> {
}
